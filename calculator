deposit = float(input())
deadline = int(input())
percent = float(input())

totalPercent = percent * 0.01

lihva = deposit * totalPercent
monthly = lihva / 12
total = deposit + deadline * monthly

print(total)
